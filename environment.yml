name: medibot-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain
      - langchain-community
      - langchain-huggingface
      - faiss-cpu
      - streamlit
      - numpy
      - python-dotenv
